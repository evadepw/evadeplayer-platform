services:
  transcoder:
    build:
      dockerfile: docker/transcoder.nvidia.Dockerfile
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,video,utility
